# 搜索方案（两路发现）

> 状态：**已定稿并已实施（2026-08-14）**。
> 关联：证据链（决策 17，已落地）解决"URL 编造"；本方案解决"该有的有没有"——即 04-问题记录手册 P-004/P-008 的质量维度。外部同类方案与社区共识模式对照见 [06-参考方案手册](06-参考方案手册.md)。

## 1. 背景与问题

现状是单路搜索：拆解 → 每个叶子节点双维度×中英文搜索 → 覆盖评估 → 薄弱节点扩量。两次实测暴露的质量缺陷：

- 交换机领域 78 条中 81% 为英文站点，中文权威源（华为/H3C/国标库/知网）随机缺失
- 候选混入凭模型记忆补写的 URL（dentOS、Stratum 事件）——证据链已解决
- 覆盖评估只看数量和体裁，从不核对"领域公认的权威源找到了没有"
- 单路浅层发现（搜索引擎 top-10 + 一次性判断）够不上"数据源发现"的质量门槛

结论：**搜索不应是唯一的发现手段**——先由领域知识点名"该有什么"，搜索负责"验证点名对不对 + 补充没点到的"，权威源核对保证底线。

**展开挖掘（WebFetch 集合页展开）已抛去，不做**，原因：搜索工具的富摘要已天然承担部分展开价值（71/78 条均来自纯搜索）；当前主要矛盾是质量不是数量；环境域名验证受限投入产出比差；下游要的是"点名权威入口"而非穷举长尾。未来环境允许时可作为可选增强恢复。

## 2. 方案总览

```
① 领域拆解（现状保留）
② 知识清单（新增）：LLM 凭领域知识列出公认权威源，按叶节点归类，URL 留空
③ 验证搜索（新增）：清单每项一次定向搜索，URL 出现即验证通过并照抄
④ 增量发现（现状保留，压缩）：双维度×中英文搜索找清单之外的源
```

- **知识负责"知道有什么"，搜索负责"证明地址是对的"**——与证据链天然衔接：验证通过的 URL 必在搜索结果里，必过 grounding 校验
- 语言生态不设专门机制：中文权威源（华为/H3C/锐捷/国标/知网）本来就躺在模型知识里，清单列出来、验证搜索自然命中

## 3. 流程设计

### 阶段 0 · 知识清单（编排层，零搜索）

- 时机：拆解完成、调用搜索层之前
- 结构：`{名称, 所属叶子节点, 预期体裁}`，**URL 留空待验证**（知识可能记错 URL，这正是 dentOS 事件的教训）
- 规模：每叶节点 2-5 条（**不含领域级条目**）；领域级权威源（IETF、Tolly 等）归到最相关节点名下；总量 ≤50
- 清单条目粒度均为"体系级"（天然满足 P-002 粒度规则，无需逐条判定）
- **清单自检**：列完后按机构类型维度自问一轮——标准组织 / 行业协会 / 厂商文档体系 / 学术文献库 / 评测机构 / 开源生态 / 百科知识库 / 公共数据平台，以及中文生态的对应物，各类都列了吗？有遗漏再补
- 预期体裁仅用于构造验证搜索词；最终 `source_type` 以验证/提取时的实际形态为准，允许修正清单预期
- 交换机示例：

```
数据中心交换机: Cisco Nexus 文档体系、华为 CloudEngine 文档体系、Gartner Peer Insights
企业园区交换机: H3C 配置指南体系、华为 CloudCampus 方案、Cisco Design Zone
工业交换机:     Hirschmann 手册、MOXA 文档、GB/T 30094 国标
以太网标准:     IEEE 802.3 工作组、IEEE 802.1 工作组、全国标准信息公共服务平台
交换芯片:       Broadcom 官网、盛科通信、TechInsights
网络操作系统:   Arista EOS 文档、Cumulus 文档
白盒交换机:     OCP、ONIE、Edgecore
开源NOS:        SONiC 官方文档、ONL
测试与评测:     Tolly、IETF RFC 2544/2889（领域级归此节点）
```

### 阶段 1 · 验证搜索（搜索层）

- 清单每项一次定向搜索：`{机构/体系名} 官方文档 / 官网`（按来源语言中英文）
- **通过标准**：搜索结果中出现该机构的官方入口 URL → 在**清单项内**填上 URL/体裁/描述/收录理由（脚本会自动并入 sources，LLM 无需在 sources 里重复写清单条目），证据链自动满足
- **清单项是体系级 → 验证 URL 必须是该体系的入口**（文档门户/目录/列表页）；只撞见体系内单份文档（PDF、新闻页、单条 KB）不算通过，扩量轮换角度专找入口（如 `{机构名} documentation`），仍找不到 → "已尽力"
- **第一轮每项只搜 1 次**：失败即标记未验证，不在本阶段重试——换角度重试统一留给扩量轮（见阶段 3）
- **未验证分两类处置**（扩量轮换角度重试后定案）：
  - 疑似幻觉/过时：换角度重试后仍搜不到该机构存在的任何证据 → 剔除出清单，报告标注"疑似无效机构"
  - 疑似搜索词问题：能搜到该机构存在、但始终找不到官方入口 → 扩量轮结束仍失败 → 留在未验证清单，标记"已尽力"
- 验证搜索顺路发现的新源 → 全量提取（同样遵守粒度规则与证据链约束）

### 阶段 2 · 增量发现（搜索层，现有逻辑压缩）

- 双维度（组织形式词 + 体裁词）× 中英文保留，每节点 **4 次**（默认词型：组织形式词中文+英文、体裁词中文+英文各一次，各取最贴节点的一个词；从 6 次压缩——有知识清单保底后，发现型搜索的边际价值下降）
- 全量提取、URL 逐字照抄（证据链约束不变）
- **粒度规则（P-002 结论，由 LLM 提取时逐条判断）**：
  - 每条候选必须声明 `granularity`：**合集级**（默认）/ **站点级** / **单篇级**
  - **合集级**：有主题边界的成体系入口（文档体系、标准库、知识库、榜单、开源仓库）
  - **站点级**：仅当整体是领域垂直门户（如工控网）时收
  - **单篇级**：仅当公认权威独立载体且无合集入口可替代时收（如团体标准文件）；白皮书/新闻稿单篇默认不收——先问"该来源有没有合集入口（白皮书列表/文档中心）？有 → 收合集入口"
  - 判定话术三连：①"持续更新/完整的知识体系，还是一次性内容？" ②"领域内有没有合集入口？有 → 收合集入口" ③"URL 形态是入口还是文档？文档形态声明体系级会被脚本拒绝"
  - 名实一致：名称声称体系级，URL 必须是该体系的入口页，不得是体系内单页
  - **脚本做确定性矛盾检测**（D11）：声明合集级/站点级 + URL 呈文档形态（PDF/新闻/问答/评测深链/联系页等确定性信号）→ 拒绝并报明细；单篇级放行但计数进 stats——例外条款的使用量每次运行可见

### 阶段 3 · 覆盖评估与收敛

薄弱节点判定标准重定义：

| 情形 | 判定 |
|------|------|
| 候选少 + 清单项有未验证 | **薄弱**——扩量轮优先把未验证清单项验证掉（每轮换一个角度：中英切换 / 全称缩写别名 / `机构名+官网`，见阶段 1），而非乱搜 |
| 候选少 + 清单项全验证通过 | **不薄弱**——权威源就这些，硬扩只会掺长尾垃圾 |
| 体裁单一 | **薄弱**（判定不变） |

"候选少"的阈值沿用现状口径：0 条，或明显少于同级节点。

终止条件：所有节点非薄弱 **且** 清单全部验证通过（或未验证项连续两轮无进展）。**扩量轮上限 2 轮优先于薄弱判定**——某节点两轮后仍薄弱（如体裁客观上单一），视为收敛并如实报告，不死循环。

### 接口契约（编排层 ↔ 搜索层）

**输入**（编排层传给搜索层）：
- `nodes`：叶子节点列表
- `knowledge`：权威源清单（含 name / node / 预期体裁；verified 默认 false，url 留空）

**输出**（搜索层返回给编排层）：
- `knowledge`：验证后的清单——每项带 `verified` 状态；verified=true 的项带齐 `source_type / url / description / reason`（可带 `granularity`，默认合集级）；未验证项带分类标注（疑似无效机构 / 已尽力）
- `new_sources`：增量发现的候选，每条含 `name / category_path / source_type / granularity / url / description / reason`
- `journal`：本次所有搜索的记录——每条含 `phase（验证搜索/增量发现/扩量轮） / node / query / results（返回链接数） / extracted（提取候选数）`

编排层把两者写入 raw.json（knowledge 原样、new_sources 并入 sources），随后调用后处理脚本。

### 搜索量预算

| 环节 | 估算 | 说明 |
|------|------|------|
| 验证搜索 | ≤50 次 | 清单每项 1 次 |
| 增量发现 | 每节点 4 次 | 10 节点 ≈ 40 次 |
| 扩量轮 | ≤2 轮 × 薄弱节点 ×（换角度验证 1-2 次 + 补搜 4 次） | 通常 ≤40 次 |

单次运行总计约 **130 次搜索**（现状 50-60 次的 2 倍以上），执行时间与上下文成本同比例上升，属**有意为之的质量成本**。若实测过重，优先砍扩量轮的补搜（薄弱节点只做换角度验证、不做增量补搜）。

### 失败路径与降级

- **清单为空**（模型对该领域知识空白）：退化为纯增量模式（阶段 2 照常执行），报告标注"知识清单为空，本次无底线保证"；脚本记清单验证 0/0
- **搜索工具持续异常**（验证搜索大面积失败）：中止并如实报告，不产出空 CSV；raw.json 保留供人工检查
- **knowledge 缺失/结构损坏**：脚本警告并记 0/0，不阻断（兼容纯增量模式）

## 4. 产出变化

- **汇总报告**增加：`清单核对: 验证通过 X/Y 项` + 未验证清单列表（**人工交接单**：供人工确认真实后自行处理，不进 CSV）
- **搜索日志**：新增 `{领域词}_{时间戳}_搜索日志.csv`——每行一次搜索（阶段/节点/查询词/返回链接数/提取候选数），供人工复盘"搜了什么、搜到了多少"。查询词由搜索层在 raw.json 的 `journal` 字段记录；脚本校验每个查询词必须逐字出现在证据留痕中（不在 → 该行"证据缺失"列标注），并写 CSV（UTF-8 BOM，与数据源清单/stats 同编码同命名模式）
- **stats CSV** 增加列：`清单验证`（如 `28/30`，领域级指标，随每行重复——同"无结果节点"列的处理方式）、`粒度矛盾移除`、`单篇级收录`
- **数据源清单 CSV** 增加列：`粒度`（合集级/站点级/单篇级，供人工按粒度抽检）
- **指标性质（重要）**：`清单验证` 衡量的是**清单自洽性**（模型列的清单里验证通过了几项）——反映幻觉/过时信号与流程稳定性，**不衡量对真实世界的完整性**。完整性评估属 P-004（2026-08-18 结案：零人力约束下外部基准不成立，见 04-问题记录手册），本指标不承担完整性测量
- **raw.json** schema 增加 `knowledge` 字段：

```json
{
  "domain": "交换机",
  "nodes": [...],
  "knowledge": [
    {"name": "IEEE 802.3 以太网工作组", "node": "以太网标准(IEEE 802.3)", "verified": true,
     "category_path": "交换机-核心技术-以太网标准(IEEE 802.3)",
     "source_type": "行业标准", "url": "https://www.ieee802.org/3/",
     "description": "IEEE 以太网标准工作组官网", "reason": "清单验证通过，官方入口"},
    {"name": "某某机构", "node": "某节点", "verified": false, "note": "已尽力"}
  ],
  "journal": [
    {"phase": "验证搜索", "node": "以太网标准(IEEE 802.3)", "query": "IEEE 802.3 Ethernet Working Group official", "results": 10, "extracted": 3}
  ],
  "sources": [...仅增量发现条目...]
}
```

**脚本自动合并**：`verified=true 且带 name/url` 的清单项由脚本自动并入 `sources`（verified 项需带齐 `category_path/source_type/url/description/reason`，缺 name/url 的脚本告警计数；LLM 不手工复制，消除"标了 verified 却漏写条目"的一致性风险）。脚本计算清单验证率为 `verified 且 name/url 均非空的项数 / 总数`（分母仅统计 dict 条目；自洽性指标，性质见上）。`knowledge` 缺失时脚本警告并记 `0/0`，不阻断（兼容纯增量模式）。

`sources` 每条含：`name / category_path / source_type / granularity / url / description / reason`（`reason` 为收录理由，粒度判定可审计；`granularity` ∈ 合集级/站点级/单篇级）。

## 5. 关键决策记录

| # | 决策 | 结论 |
|---|------|------|
| D1 | 验证失败的清单项放哪 | 只进报告"未验证清单"，不进 CSV（与证据链原则一致） |
| D2 | 清单写进 raw.json 作为核对表 | 要——脚本计算"清单验证 X/Y"进 stats，A/B 一次落地 |
| D3 | 展开通道降级 | 取消（展开整体抛去） |
| D4 | 清单规模上限 | 总量 ≤50 |
| D5 | 增量发现搜索次数 | 每节点 6 次压缩到 4 次 |
| D6 | 验证与增量的顺序 | 先验证后增量——底线先钉死，中断也不亏 |
| D7 | 清单验证率进 stats 形式 | 领域级指标"清单验证 X/Y"随行重复 + 未验证清单进报告；指标性质标注为自洽性（见第 4 节） |
| D8 | 粒度规则（P-002 结案） | 三层都有、合集级为主；LLM 提取时逐条判定（判定话术写进搜索层规则），候选带收录理由；脚本不做粒度预标 |
| D9 | 域名一致性规则 | **不采纳**（2026-08-14 讨论）——验证通过维持现状：URL 出现在搜索结果即算通过，不额外约束域名归属；转载/镜像风险接受，靠粒度规则与人工抽检兜底 |
| D10 | 搜索日志 | 搜索层记录 journal（阶段/节点/查询词/条数），脚本校验查询词必须在证据留痕中（不在标注"证据缺失"）并落盘 `搜索日志.csv`——日志本身受证据链约束 |
| D11 | 粒度矛盾脚本检测（2026-08-17） | 实测 85 条产物中约 1/4 为"单篇撞见/名实不符"（清单验证用单份 PDF 当入口、白皮书新闻稿单篇大量进入）。结论：候选强制声明 `granularity`（合集级/站点级/单篇级）；脚本对"声明合集级/站点级 + URL 呈文档形态（PDF/新闻/问答/评测深链/联系页等确定性信号）"做矛盾拒绝——名实不符类在结构上杜绝（保守口径，宁可漏检不误杀榜单/门户）；单篇级放行但计数进 stats，例外滥用每次运行可见；验证通过标准补粒度匹配（清单项体系级必须找到入口 URL 才算通过）；数据源清单 CSV 加"粒度"列 |

## 6. 落地改动清单

| 文件 | 改动 |
|------|------|
| `.claude/skills/autosource/SKILL.md` | 拆解后新增"阶段 0 知识清单"步骤；raw.json schema 增加 knowledge；汇总报告格式增加核对段 |
| `.claude/skills/autosource-search/SKILL.md` | 输入增加权威源清单；新增"阶段 1 验证搜索"；增量发现 4 次/节点；**移除集合页展开段落**；薄弱判定与终止条件重写；按接口契约定义输入/输出结构（含 journal 记录） |
| `.claude/skills/autosource/postprocess.py` | knowledge 清单验证率计算；verified 清单项自动并入 sources；journal 校验（查询词须在证据留痕中）并写搜索日志.csv；stats CSV 新列"清单验证"；stdout 打印未验证清单；knowledge 缺失容错 |
| `tests/test_postprocess.py` | 覆盖 knowledge 与 journal 场景：验证率计算、自动并入、搜索日志校验与生成、未验证清单输出、knowledge 缺失容错 |

## 7. 验收方式

1. 单元测试全过
2. 重启会话（hook 生效）后跑 `/autosource 交换机`：汇总报告出现"清单核对: 验证通过 X/Y"；与上次 78 条对比，中文权威源（华为/H3C/国标/知网等）应自然进入清单并验证通过；输出目录出现搜索日志.csv，可完整复盘每次搜索的查询词与提取数
3. 在清单里放一个不存在的机构：换角度重试后应被剔除出清单并在报告标注"疑似无效机构"；放一个真实但难搜的机构：换角度仍失败后应留在"未验证清单"标记"已尽力"——两种情况都不进 CSV
