# AutoSource 问题记录手册

> 用途：逐项核对 AutoSource 的实际设计是否服务于“发现公开知识源”的上游任务；记录讨论结论，而非预设现有方案有问题。
>
> 边界：本手册只讨论发现阶段——识别并输出可供下游消费的来源 URL 及必要描述。下载、页面遍历、解析、清洗、抽取等抓取细节属于下游，不在本手册范围。

## 已确认的产品定义

- **目标**：自动发现某个领域内公开的互联网知识源，并输出来源清单，供下游任务按 URL 抓取具体内容。
- **发现对象**：不局限于数据集；包括官方文档、产品说明、技术手册、知识库、开放标准、论文、API 文档、帮助中心等。
- **准入边界**：资源应公开可访问。该条件是发现阶段的筛选前提，不应展开成下游抓取策略。
- **关注重点**：是否找到了应覆盖的来源、来源是否相关、重复来源是否被合理归并、输出是否足够让下游识别来源。
- **不讨论**：如何抓取、抓取哪些页面、反爬应对、内容解析、清洗与入库。

## 本手册如何记录问题

每个问题使用下列字段：

| 字段 | 含义 |
| --- | --- |
| 编号 / 状态 | 便于追踪；状态为“待核查 / 讨论中 / 已决定 / 暂缓”。 |
| 问题 | 需要作出的产品或架构判断。 |
| 已知事实与证据 | 来自需求、代码、测试或实际运行结果；没有证据时明确标注。 |
| 影响 | 不解决会造成的发现结果问题。 |
| 待讨论 | 需要先达成一致的判断点。 |
| 结论 | 讨论后填写；未达成一致时保持为空。 |
| 后续动作 | 只有结论明确后才添加实施或验证动作。 |

## 关于“来源元数据”

这里的元数据不是“如何抓取”的提示，也不是下游抓取清单。它只是在一条来源 URL 旁，保留**识别、展示、去重和筛选该来源所必需的描述**。

建议先按最小集合理解，是否保留由后续讨论决定：

| 字段 | 示例 | 用途 |
| --- | --- | --- |
| `url` | `https://docs.example.com/product/` | 下游任务的输入和来源唯一定位。 |
| `title` | `Example Product Documentation` | 让人和程序识别来源名称。 |
| `source_type` | `official_docs`、`manual`、`standard`、`paper` | 区分知识源类别，便于筛选和评估覆盖面。 |
| `scope` | `Example 产品的开发与运维文档` | 说明该 URL 覆盖的主题或内容集合。 |
| `publisher`（可选） | `Example Inc.` | 识别发布主体，辅助判断权威性与去重。 |

明确**不属于发现阶段输出**的内容：抓取方式、抓取深度、应抓页面列表、解析规则、反爬策略、登录方式、清洗规则等。

---

## 问题清单

### P-001：项目概念是否仍被“数据集发现”限制

- **状态**：已决定（结案）
- **问题**：当前术语、分类、搜索策略和筛选标准，是否把“数据源”狭义地实现成数据集，而未覆盖广义知识源？
- **已知事实与证据**：产品目标已明确为公开知识源发现；实测两次运行（算力服务器 71 条、交换机 78 条）体裁分布证明输出已不限于数据集——数据集占比 <10%，以官方文档/行业标准/开源生态/评测平台/论文专利为主，覆盖 12+ 种体裁。
- **影响**：如果项目仍以数据集为中心，官方文档、手册、标准和产品知识库等高价值来源可能根本不会被发现，或被错误过滤。
- **待讨论**：哪些来源类型是第一阶段必须支持的；哪些类型可以暂缓。
- **结论**（2026-08-14）：实现已覆盖广义知识源，不以数据集为中心，无需改动；P-002 粒度规则（合集级为主）落地时自然巩固广义载体标准。
- **后续动作**：无。

### P-002：发现结果的最小粒度尚未确定

- **状态**：已决定
- **问题**：一条“数据源”究竟代表站点/域名、内容集合，还是单篇页面/单个文件？
- **已知事实与证据**：下游按 URL 抓取；发现阶段的对象可以是文档库、某版本手册集合、标准库，也可能是单篇 PDF 或页面。实测交换机 78 条清单三种粒度混杂，单篇级条目（CSDN 对比表、fibermall 博客、单份 PDF）与合集级条目（文档体系、标准库、榜单）无规则并置。
- **影响**：粒度会直接影响搜索词、去重方式、输出数量和下游入口。
- **待讨论**：是否采用“三层都有、以内容集合级为主”的模型；如果采用，三层之间如何关联和去重。
- **结论**（2026-08-14）：**三层都有、合集级为主**——默认只收内容集合级（有主题边界的成体系入口：文档体系/标准库/知识库/榜单/开源仓库）；站点级仅当整体是领域垂直门户（如工控网）时收；单篇级仅当是公认权威独立载体且无合集入口可替代（如行业白皮书）时收。判定由 LLM 在提取时执行（语义判断，判定话术：“这个 URL 背后是持续更新/完整的知识体系，还是一次性内容？一次性内容不收。”），每条候选带 `reason` 收录理由供人工抽检审计；脚本不做粒度预标。
- **后续动作**：已写入 02-搜索方案.md（阶段 2 + 决策 D8）；实施时在搜索层 SKILL.md 落实判定话术与 `reason` 字段。

### P-003：输出描述应服务于来源识别，而非下游抓取

- **状态**：已初步澄清
- **问题**：发现任务需要输出哪些最小字段，才能让下游根据 URL 使用结果，同时不侵入下游职责？
- **已知事实与证据**：已明确不需要抓取提示、页面清单、抓取深度或解析规则；上文列出了仅用于来源识别的候选字段。
- **影响**：字段过少会使结果难以判断与去重；字段过多会把下游责任混入上游，增加不必要复杂度。
- **待讨论**：`title`、`source_type`、`scope` 是否为必填；`publisher` 是否暂定为可选。
- **结论**：暂定以 `url` 为必填，其余字段在讨论后确定。
- **后续动作**：P-002 已定（2026-08-14）；字段集按现 raw.json schema（`name / category_path / source_type / url / description / reason`）待最终确认。

### P-004：如何定义“发现得足够好”

- **状态**：已决定（结案）
- **问题**：自动发现无法证明覆盖互联网全部资源时，用什么标准评价结果是否足够完整、相关和可信？
- **已知事实与证据**：目标是产出供下游消费的候选 URL；目前尚未确认项目是否已有覆盖率、重复率或人工抽检机制。2026-08-14 补记：两路搜索方案中的"清单验证 X/Y"指标为**自洽性指标**（模型列的清单→模型搜索验证，存在循环论证），不衡量对真实世界的完整性；完整性仍需模型外部基准（人工构建权威源全集测召回 + 人工抽检测精确性）。
- **影响**：没有明确评价标准时，搜索轮次、分类扩展和过滤规则无法稳定迭代。
- **待讨论**：是否先以“各必需来源类型均有候选 + 人工抽检相关性 + URL 去重”为第一阶段标准，而不追求不可验证的全网穷尽。
- **结论**（2026-08-18）：在"零人力投入"约束下，外部基准不成立——外部真值（权威源全集/抽检判定）只能来自人，而"每领域找专家"与工具"替代人工"的目标直接矛盾。候选替代方案逐一评估并否决：① 人工基准（全集/抽检）违背零人力约束；② 相对基准（以上次运行产物为基线）自我锚定，初始偏差逐轮强化并极化已跑领域；③ 持久策展池（回灌自身历史产物）与②同构的极化机制，叠加新领域冷启动（池为空）与时效性维护成本；靠外部收割生长的池受当前网络环境限制不可行。跨模型/外部策展内容的本质是"信息通道"——当裁判用浪费杠杆，应直接并进发现流程。**最终口径："找得全"的保证由通道多样性承担而非外部测量**——通道是发现输入不是裁判；通道交集=多源确认（置信度），差集=盲区信号（改进目标）。
- **后续动作**：无。现有 `stats` 指标（清单验证率/单篇级收录/体裁分布）继续作为过程健康度指标，口径不变。

### P-005：关键确定性环节未代码化，由 Claude 手工执行

- **状态**：已决定
- **问题**：CSV 规范化生成、`stats` 写入、中间文件清理等确定性环节，应由代码保证，还是继续交给 Claude 手工执行？
- **已知事实与证据**：全仓库无生成 CSV / 写 stats 的代码，`postprocess.py` 只产出 `clean.json`；`SKILL.md` Step 5/6 为自然语言指令。实测：2026-08-12 运行无 `stats.json`；2026-08-13 的 `stats.json` 中 `"model"` 为占位符原文，字段与脚本 stdout 口径不一致（脚本算出的去重数 / 无效 URL 丢失）；`clean.json` 导出后即删，无法审计 CSV 是否忠实反映脚本过滤结果。
- **影响**：产出格式、完整性、可审计性均依赖 Claude 每次正确执行，不可复现；违反 NFR 5.5（效果数据）与可迭代性要求。
- **待讨论**：将 CSV 导出 + stats 写入下沉进脚本（用 `csv` 模块保证 BOM 与转义），Claude 只负责发现与拆解；是否保留中间产物用于审计。
- **结论**：按"确定性逻辑代码化、LLM 只做语义推理"原则，全部下沉进 `postprocess.py`：脚本负责时间戳与目录命名、去重、CSV 导出（csv 标准库 + UTF-8 BOM）、stats 计算与写入（含空节点检测、处理耗时）、raw.json 清理；Claude 只产 raw.json。中间产物默认删除，提供 `--keep-raw` 参数保留供审计。
- **后续动作**：已完成——`postprocess.py` 重写 + 18 个单元测试 + SKILL.md 流程同步（2026-08-13）。

### P-007：后处理健壮性不足——脏数据即崩溃，去重口径有盲区

- **状态**：讨论中
- **问题**：后处理如何对不完整输入容错？去重键是否需要归一化？
- **已知事实与证据**：`postprocess.py:25` 直接下标取 `name` / `url`，缺字段即 `KeyError`、整批中断（实测），`process()` 无任何容错。去重 `_domain()` 用完整 `netloc`，`www` / 非 `www`、大小写、末尾斜杠、名称未归一化会漏合（实测 `nvidia.com` 与 `www.nvidia.com` 被留为两条）。次要项：串行校验慢（31 条实测 143s）、GET 分支连接未 `close()` 泄漏、重试无退避。
- **影响**：脏数据可致整次运行异常无产出（违反 AC-1）；去重存在盲区（NFR 5.2）。
- **待讨论**：对缺 `name` / `url` / 空值记录跳过并计数上报；去重键做 host 归一化（去 `www.`、小写化）；是否并发化校验。
- **结论**：待讨论。
- **后续动作**：结论确定后修 `postprocess.py` 并补测试。

### P-008：产量不达标，且流程步骤缺乏强制保证

- **状态**：讨论中
- **问题**：如何稳定达到 AC-2（≥50 条）？哪些“逻辑步骤”应从提示词下沉为代码 / 脚本兜底以保证可复现？
- **已知事实与证据**：2026-08-13 / 2026-08-12 产出 31 / 10 条，均 < 50，忠实校验后 08-13 降至约 27。领域拆解、扩量终止（`autosource-search/SKILL.md:64-66`）、无结果降级、写 stats、清理中间文件全为提示词、无强制。实测两次运行质量与格式悬殊：08-12 严重违反“禁止产品名”规则（满是 NVIDIA 产品页 / 对比）且无 stats，08-13 相反偏榜单。
- **影响**：违反 AC-2；可复现性差。
- **待讨论**：对扩量给出可量化的最低产出 / 收敛保证；能代码化的（终止阈值、降级重试次数、stats、清理）尽量代码化；“禁止产品名”等硬约束做成搜索层检查项。
- **结论**：待讨论。
- **后续动作**：评估产量与扩量策略。

## 讨论日志

- 2026-08-13：确认项目是**上游知识源发现**，下游抓取不在范围内；建立本手册并登记 P-001 至 P-004。
- 2026-08-13：完成独立可行性审查（通读代码 + 两次真实产出 + 对导出 CSV 的 31 个 URL 实测），登记 P-005 至 P-008。总体判断：当前尚不能可靠实现自动发现——关键确定性环节（CSV / stats）未代码化、URL 校验会误杀真实链接且不可复现、产出量未达 AC-2、脏数据可致后处理崩溃。建议修复顺序：P-006（止损）→ P-005（代码化并保留中间产物）→ P-007（容错）→ P-008（产量与流程保证）。
- 2026-08-13：为聚焦发现能力，URL 可达性校验已在另一会话临时移除（`postprocess.py` 现只去重）；**P-006 随之移除**，P-008 中对 P-006 的引用一并清理。修复顺序相应调整为 P-005 → P-007 → P-008；URL 校验待发现能力打磨到位后再重新设计。
- 2026-08-13：stats 输出格式由 JSON 改为 CSV（`{领域词}_{时间戳}_stats.csv`，UTF-8 BOM，每行一个分类节点），命名与数据源清单 CSV 同步，便于 Excel 打开与多次运行横向对比；运行目录时间戳改由脚本获取真实时间（此前模型编造时间戳，实测偏差约 3 小时）。P-005 中"stats 写入是否下沉进脚本"的讨论不受影响。
- 2026-08-13：**P-005 结案**——按"确定性逻辑代码化、LLM 只做语义推理"原则，CSV 导出 / stats 统计 / 文件清理 / 时间戳全部下沉进 `postprocess.py`，LLM 唯一中间产物为 raw.json（`--keep-raw` 可保留审计）；单元测试 9 → 18 个；SKILL.md 流程同步。顺带落地 P-007 待讨论中的"脏记录（缺 name/url）跳过并计数"一项；去重键归一化（www/大小写）仍未做，P-007 保持讨论中。
- 2026-08-14：**新增证据留痕 + grounding 校验**——交换机领域实测发现候选清单中混入两条凭模型记忆补写的 GitHub URL（dentOS、Stratum，事后查证恰好真实，但流程无任何把关）。结论：指定搜索工具只约束工具调用，管不住工具调用间隙生成文本的字段级幻觉；提示词约束不可强制。按行业标准实践（工具调用留痕 + groundedness 校验 + 确定性护栏层）落地：`log_tool.py` + PostToolUse hook 由 harness 系统记录每次 WebSearch/WebFetch 原始结果，`postprocess.py` 校验候选 URL 必须逐字出现在留痕中，否则拒绝并计数；留痕缺失即拒绝处理。单元测试 18 → 25 个。平台迁移性留作后续话题（hook 是 Claude Code 专属机制，策略/契约/脚本本身跨平台）。
- 2026-08-14：**P-002 结案**——粒度定为"三层都有、合集级为主"：默认只收内容集合级，站点级/单篇级仅在明确例外条件下收；判定由 LLM 提取时执行（判定话术写入搜索层规则），候选带 `reason` 收录理由供审计，脚本不做粒度预标。已同步 02-搜索方案.md（阶段 2 + D8）。P-003 字段集随之基本固定，待最终确认。
- 2026-08-14：**P-004 补记指标边界**——两路方案中的"清单验证 X/Y"指标定性为自洽性指标（模型列→模型验，循环论证），不衡量对真实世界的完整性；stats 列名由"权威源覆盖"改为"清单验证"如实表述。完整性评估仍依赖外部基准（人工全集/抽检），悬置中。
- 2026-08-14：**验证通过标准维持现状**——讨论"域名一致性规则"（验证通过要求 URL 域名属于机构主体、转载不算），结论**不采纳**：维持"URL 出现在搜索结果即通过"，不额外约束域名归属；转载/镜像风险接受，靠粒度规则与人工抽检兜底。已记入 02-搜索方案.md 决策 D9。
- 2026-08-14：**搜索方案交叉评估收尾**——P-001 结案（实测体裁分布证明已泛化）；02-搜索方案.md 按评估修订 B1-B6：knowledge→sources 改脚本自动合并（消除一致性风险）、验证失败分"疑似幻觉/搜索词问题"两类处置并定换角度规则、阶段 0 补清单自检（按机构类型维度）、终止条件补"两轮上限优先于薄弱判定"、预期体裁允许修正、领域级条目不计入节点配比、未验证清单定位为人工交接单。
- 2026-08-14：**方案规格完备性复审**——六项修订：换角度重试轮次归属明确（第一轮每项 1 次，重试归扩量轮）、验收方式与新规则对齐、新增接口契约（编排层↔搜索层输入输出结构）、新增搜索量预算（~130 次/运行 + 削减策略）、新增失败路径与降级（清单为空退纯增量/工具异常中止/knowledge 缺失容错）、顺路新源同样受粒度规则约束、"候选少"阈值引用现状口径。
- 2026-08-14：**新增搜索日志功能（D10）**——应需求增加人工可读的搜索日志：搜索层在 raw.json 的 `journal` 字段记录每次搜索（阶段/节点/查询词/返回条数/提取条数），脚本校验查询词必须逐字在证据留痕中（不在则标注"证据缺失"）并落盘 `{领域词}_{时间戳}_搜索日志.csv`；日志本身受证据链约束，供复盘"搜了什么、搜到了多少"。
- 2026-08-14：**两路搜索方案实施完成**——编排层新增知识清单步骤与 raw.json 新 schema（knowledge/journal）；搜索层新增验证搜索、压缩增量发现为 4 次/节点、粒度规则落地、新薄弱判定与终止条件、移除集合页展开、按接口契约输出；postprocess.py 新增清单并入（merge_knowledge）、清单验证率、journal 校验与搜索日志.csv、stats"清单验证"列；单元测试 25 → 32 个全过。待重启会话后验收（hook 生效 + /autosource 交换机 实测）。
- 2026-08-14：**实施审查修复**——subagent 审查发现 1 高/2 中/7 低，已修复：失败路径落地（搜索层声明 failed 状态 + 编排层中止 + 脚本兜底"journal 非空且 0 候选即中止、raw.json 保留"）；journal 损坏条目计数告警；sources 非 list 容错；清单验证率定义对齐（name/url 均非空、分母过滤非 dict）；措辞统一（"验证通过 X/Y 项"、补"无底线保证"语义）；接口映射明写（knowledge/new_sources→sources）；WebFetch 从搜索层 allowed-tools 移除；扩量轮补搜范围明确。测试补 9 个 → 41 个全过。已知近似记录：证据链"逐字"匹配当前为子串匹配（URL 前缀可通过），后续可加固为边界匹配。
- 2026-08-17：**端到端验收运行 + 复盘修复**——/autosource 交换机 首次完整跑通两路方案 + 证据链：清单 36/36 全验证（含 OCP 规格库扩量轮换角度通过）、77 次搜索全留痕（journal 0 证据缺失）、88 条产出、无空节点。证据链真实拦截 1 条转写错误（华为 CloudEngine：support.huawei.cn 被写成 .com）——机制按设计工作，模型 URL 转写错误率实测 ≈1%（1/89）。复盘发现三处问题并修复：① 证据校验拒绝时 stdout 只报数字不给明细 → 增加被拒明细打印（名称+URL）；② 被拒条目无恢复路径 → 有拒绝时自动保留 raw.json 与证据留痕，修正后可重跑；③ Windows 控制台 GBK 乱码 → stdout/stderr 统一 UTF-8。单元测试保持 41 个全过（check_grounded 签名改为返回被拒列表）。另：排障 Bash 命令弹窗问题，将 ls/wc/grep 只读命令加入 settings 白名单。
- 2026-08-17：**通用平台条目不作为独立条目（P-002 粒度规则的延伸）**——验收产物中混入中国知网（cnki.net 首页）、Google Patents（随机专利页）、全国标准信息公共服务平台等跨领域通用平台条目。判定：无主题边界、违反 P-002；平台首页 URL 下游采集不到领域内容（把发现任务推回下游）；人人皆知的平台是噪声。结论：① 清单与提取两层加准入判据"条目的 URL 必须定位到该领域的内容"——平台首页/根域名不收，平台的领域专属入口（该领域分类检索页/带领域词检索页）可收；② 平台信息通过领域条目的 description 传递；③ 跨领域恒定检索入口写入 05-交付手册第 2 节一次，不随每次运行重复输出。规则为 LLM 提取时执行的语义判断（与 P-002 同一层级）；若实测仍漏入，再评估 postprocess 域名黑名单兜底。本次 88 条产物清理 3 条（知网/Google Patents/全国标准平台）。
- 2026-08-17：**粒度执行失效复盘——"单篇撞见"与"名实不符"（P-002 补强，D11）**——清完平台条目后的 85 条中仍约 1/4 为搜索撞见的单篇：清单验证用体系内单份 PDF 当入口（Hirschmann"手册体系"→单 PDF、NETGEAR"支持中心"→单条 KB、盛科官网→contact_us）；增量提取把白皮书/新闻稿/评测单篇当候选（华为 AI 白皮书新闻页、ServeTheHome 单篇评测等）；SONiC 手册/CLI 单文件与官方文档站重叠。归因：① 提示词约束不可强制——粒度判断与 URL 转写同类，发生在工具调用间隙的字段级生成；② 例外条款"公认权威独立载体"被宽松执行为常态；③ 缺测量机制，偏差不可见。分层修复：候选强制声明 `granularity`；脚本对"声明合集级/站点级 + URL 文档形态（确定性信号：扩展名/新闻/问答/评测深链/联系页/GitHub 子页）"做矛盾拒绝——名实不符子类结构上杜绝（保守口径，不误杀榜单/门户，有拒绝时保留 raw.json 可修正重跑）；单篇级放行但计数进 stats（"例外是否变成常态"可见）；验证通过标准补粒度匹配（清单项体系级必须找到入口 URL）；判定话术升级三连 + 单篇级 reason 必写例外依据。边界（已明确）：单篇级最终判定仍是 LLM 语义判断，结构保证只覆盖矛盾类。单元测试 41 → 64 个全过。
- 2026-08-17：**补流程纪律：禁止手工证据核对**——排障发现运行中 LLM 用 for/while 循环复合命令做 grep 证据预检（触发权限弹窗；白名单只有单命令前缀规则，复合命令不匹配）。根因是分工原则缺口：证据校验是脚本职责（含恢复路径），LLM 不该手工预检。修法：编排层与搜索层 SKILL.md 加纪律"证据核对不手工做，直接跑脚本、被拒后走恢复路径；调查用 Read 工具"；**不**把 for/while 加入白名单（循环可包任意命令，危险面大）。
- 2026-08-18：**P-004 结案**——确认约束：基准机制不允许任何人力投入（领域多，不可能每领域找专家）。逐一论证：人工全集/抽检（违背零人力）、相对基准（自我锚定极化）、持久策展池（极化+冷启动+时效性）、跨模型/策展收割当裁判（渠道当裁判浪费杠杆）均不成立。结论：零人力下绝对召回不可测（接受）；"找得全"的保证转向通道多样性（通道是发现输入不是裁判，交集=多源确认、差集=盲区信号）。本轮不实施，仅结案存档。
- 2026-08-18：**证据链加固（边界匹配）+ 措辞精确化**——评估指出"结构上不可能"说法略高于实际：① 子串匹配下 URL 截短为父路径/裸域名可通过（留痕有 `https://a.com/doc/123` 时 `https://a.com` 能过）；② 留痕文件无写保护，理论可被模型写入。结论：防的是意外编造，不防对抗性篡改——对自用工具足够，措辞改精确。加固：`check_grounded` 改边界匹配（RFC 3986 字符集判定前后字符，截短拒绝），journal 查询词改精确集合比对（解析 JSONL 收集字符串值，截短/改写标"证据缺失"），SKILL.md×2 / 05 手册 / log_tool.py 措辞同步；单元测试 64 → 74 个全过。已知近似：边界字符集含句号/括号等标点，自由文本中 URL 紧跟标点（如句号结尾、markdown 链接）的场景会误拒——实际流程中候选均来自结构化搜索结果（引号包围），不受影响。
- 2026-08-18：**权限白名单收敛（运行期/开发期分层）**——评估指出 `Bash(python -c *)` 等价任意代码执行、与"不把 for/while 入白名单"的安全推理不一致，且 settings.json 随 git 分发构成供应链风险面（工具摄入大量不受信网络内容，白名单是指令注入的唯一防线）。用户澄清放权初衷：运行过程中弹窗等人确认不合理（违背自动化目标）。结论：区分运行期与开发期——运行期流水线仅需 搜索/Read/Write+Edit(outputs/**)/postprocess 规则，给足即零弹窗；开发期便利项（python -c/pip install/git/rm/mkdir/cd/ls/wc/grep/pytest 等）移到 settings.local.json（gitignore，本机会话三层并集生效，开发摩擦零变化）。settings.json 收敛为流水线最小集，local 补 Write/Edit(项目目录)；搜索层 allowed-tools 移除 Bash（展开挖掘时代遗留，当前流程无合法 Bash 用途）。权限修改需新会话生效（与 hook 同理）。
- 2026-08-18：**文档计数单一事实源**——README/03/05 的测试计数与 02 状态标注多次漂移（25/32/41/64 vs 实际 74，各冻结在最后一次更新的时刻）。修法：测试数只在 README 测试节保留一处，其余文件去硬编码（"单元测试全过"或指向 README）；02 状态统一为"已定稿并已实施"。
- 2026-08-18：**中间产物默认归档（run bundle 补完）**——原设计中间产物默认删除、`--keep-raw` 保留供审计（P-005 结论）；组长反馈"保留更多中间日志方便调试"+ D11 粒度复盘时无旧 raw.json 可用的实际教训，改为：全部通过时 raw.json 与证据留痕拷入运行目录归档（时间戳命名，输入+日志+产出+指标同目录不可变保留）；有被拒条目时**不生成输出目录**（不落任何 CSV、不归档，一次调用至多一个目录且必然是完整版），raw.json 与留痕原件保留供修正重跑，`--keep-raw` 语义不变。依据：数据工程 run bundle 惯例。方案评审结论：归档由脚本全包（不引入 LLM 写文件，守住 P-005 原则）。已知近似：证据留痕为会话级文件（含同会话其他运行与无关搜索），如实命名不切片，切片留作以后；产物增长无上限，单人使用量级可忽略。
- 2026-08-19：**URL 引用锚点清理**——实测产物 URL 带 `#3#1` 双尾巴（WebSearch 结果 markdown 渲染的引用序号），逐字照抄纪律的必然副作用：边界匹配主动惩罚去尾巴（抄 `#3` 被判截短），机制逼着模型照抄带锚点形态。方案评审确认非临时补丁：① 修机制不修数据（每轮必然再生的产物）；② 判据是 URL 语义事实（fragment 不发给服务器，剥掉不改变资源指向）非启发式；③ 层级正确（P-005：确定性清理入脚本）。落地：`strip_citation_anchors` 在证据校验通过后、写 CSV 前剥离尾部纯数字锚点（`(#\d+)+$`），单词锚点（#content）保留；证据链校验保持严格逐字不动；搜索层规则补"照抄即可，不要自行去尾巴（会被判截短）"。排除方案：hook 过滤证据（篡改审计日志）、放宽边界匹配（输出仍脏且削弱校验）、提示词约束（不可强制）、下游清理（职责错位）。单元测试 77 → 83 个全过。
- 2026-08-19：**中间产物归档升级为工程化 run bundle**——用户对平铺归档+会话级留痕不切片不满意，对照数据工程/ML 实验管理惯例（MLflow/W&B/DVC）重构：① 交付物在运行目录根、中间产物入 `intermediate/` 子目录（分层）；② 新增 `manifest.json` 运行清单——每个文件相对路径/用途/生成方/sha256 校验和，归档不可变性与可审计性可脚本验证（与证据链哲学一致）；③ 证据留痕按运行切片——只保留 `tool_input.query` 命中本运行 journal 查询词集合的行（复用精确比对机制，会话级 → 运行级，消掉"不切片"已知近似）；④ 保留：JSONL 结构化日志、时间戳命名、不可变归档、拒绝时原件保留、`--keep-raw` 语义。已知近似：journal 缺行则该次搜索不进切片（如实标注）。单元测试 83 → 88 个全过。
- 2026-08-19：**行级数据血缘（data lineage）落地**——用户审计时发现溯源断链："这条数据源出自哪个搜索词"与"这个搜索词返回了什么、收录了哪几条"均不可查（文件级有 manifest、搜索级有 journal，行级缺关联键）。方案评审确认：① 血缘是确定性推导不是语义记忆——LLM 逐条记来源会制造"错但可通过校验"的新幻觉面，否决；② 关联键已存在（源 URL ↔ 留痕结果 URL），脚本 join 生成，LLM 零新增职责；③ 单列承载不了多出处（实测同 URL 在 9+ 行留痕出现）——"来源搜索"列取首次出现（≈发现时刻，确定性），完整多出处入血缘表。落地：数据源清单加"来源搜索"列（7 列）；新增 `溯源.csv`（每行一条结构化结果：阶段/节点/查询词/返回结果数/结果URL/是否收录/收录条目名称/收录理由/备注），收录理由从 kept 源带入，仅摘要文本的 URL 走回退行标注；进 manifest。已知边界：未收录结果的排除理由未记录（提取时未留）；journal 缺行则该次搜索不进切片（同上条）。单元测试 88 → 92 个全过。
- 2026-08-20：**恢复路径四层修法（弹窗根因治理）**——网络安全轮排障发现运行会话 6 条 Bash 全部越界（自创 build_raw.py 重建脚本 ×2、python -c 手工核对留痕 ×3、rm/ls 清理复合命令 ×1），权限弹窗是白名单最小集按设计拦截越界行为（护栏报警，非缺陷）；同时再次实证"散文禁令不可强制"（08-17 已禁手工核对，08-20 照做）。对照 2026 企业级主流（bounded retries + 错误反馈 + escalate to human + SLO）落地四层：① 拦截——白名单一条不加，弹窗保留（PoLP）；② 信号——被拒时退出码 + 一句话动作指引（含剩余轮次）；③ 闸门——`.recovery_rounds.json` 标记文件按 domain 记录修正轮次（≤2 轮），"升级给人类"由删除标记文件表达（代码闸门而非散文）；④ 预期——SKILL 恢复路径重写（禁止清单 + 轮次上限）。对照行业 SLO：实测拒绝率 ~1%（健康区，行业阈值 ~5%）。单元测试 92 → 98 个全过。**当日再修订见下条（中止 → 隔离）。**
- 2026-08-20：**拒绝处置从"中止"修订为"隔离"（无人值守定调）**——用户明确最终形态必须无人值守产出产物，"连续失败即中止"违背该要求（闸门只管优化循环、不该当生产开关）。对照无人值守流水线标准（数据仓库 reject table / Kafka DLQ）：坏记录隔离、主表照常产出。落地：postprocess 永不因被拒而中止——被拒条目进 `被拒记录.csv`（列与交付物对齐 + 拒绝原因，无被拒不生成，进 manifest），其余 bundle 完整产出；修正轮次标记只管可选优化循环（≤2 轮，用尽仍产出、仅 stdout 提示升级）；被拒轮次保留 raw.json 与留痕、零被拒才清理与清标记；退出码统一为产出即 0。拒绝可见性三层：stdout 即时、stats 计数（证据校验移除/粒度矛盾移除）、被拒记录明细。方案评审对比：并入主表加状态列被否（主表污染、误用陷阱）；并入溯源被否（结果级 vs 候选级语义错乱）——单独隔离桶与行业 DLQ 惯例一致。测试保持 98 个全过。
- 2026-08-20：**溯源表检查修正（网络安全产物实测）**——对网络安全轮溯源.csv 做三层交叉检查，发现并修复：① 结果URL 列漏剥引用锚点（497/1239 行带 #1 尾巴）——build_lineage 写入时统一剥离，并新增 TestUrlHygiene 不变量断言（清单+溯源两列无纯数字锚点，防止新输出面再漏）；② "是否收录"列未收录行为空而非显式"否"（用户指出）——布尔状态列空值只该表示"不适用"，此处必填 是/否；同惯例修正搜索日志"证据缺失"列；③ 如实差异文档注明：journal"返回结果数"（LLM 约数）与结果行数（证据真实数）可差 ±1、顺路发现的源跨节点归因。不采纳项：候选 URL 必须 http(s) 开头（用户定调不硬化——证据里混入的非 http 垃圾由 LLM 粒度判断挡住）。另：项目根新建 CLAUDE.md（四条流程纪律：技能前置调用/TDD、不变量级验证标准、约定钉测试、先讨论后实施），并写记忆条目（技能靠"记得调用"不可靠的教训）。测试 98 → 100 个全过。
- 2026-08-20：**大幅简化被拒流程（用户决策：产量优先 + 溯源已足够）**——用户逐层审视后定调：① 规则二（粒度矛盾检测）删除——"现在产量太少"，粒度/子站问题留给用户后续自研的"站点与子站合并"功能；granularity 声明保留为信息展示（清单"粒度"列 + 单篇级收录计数），不再拒绝；② 被拒记录.csv 删除——"有溯源就够了"，被拒明细只在 stdout 打印、计数在 stats；③ 修正重跑环节删除——被拒即结束，LLM 无后续动作，弹窗从结构上不可能（此前四层修法的散文纪律已三度失效：08-17 禁手工核对、08-20 早禁自创脚本，软件工程轮仍复现 build_raw.py/update_scratch2.py/python -c 越界）；轮次标记文件一并删除。简化后的后处理：证据校验（被拒不进清单、其余照常产出完整 bundle）→ 清单并入 → 去重 → CSV/stats/溯源/manifest → 清理。测试 100 → 79 个全过。
- 2026-08-20：**raw.json 组装归位（build_raw.py 事件的结构修复）**——同日另一次运行再次实测编排层自创 build_raw.py + search_scratch.json 用脚本组装 raw.json（动机：52KB 大 JSON 手抄不可靠，脚本内含字段映射与剥 expected_type），执行时被白名单弹窗拦截；"禁止自创脚本"散文纪律第四次失效，且证实此前"简化后弹窗从结构上不可能"的结论只覆盖修正重跑动机、未覆盖组装动机。复盘定论：组装 raw.json 是确定性环节（字段映射/剥内部字段，无判断成分），按 P-005 划线本就该归脚本侧——"编排层 LLM 转录搜索层文本"是架构把确定性活压在 LLM 身上的缺口。落地：搜索层按契约直接用 Write 落盘 outputs/raw.json（文本返回只报状态；失败路径不写文件），编排层零文件写入、零转录；postprocess 消费不变（manifest 生成方改"搜索层"）；契约外字段容错（expected_type）钉进测试。测试 81 个全过。
- 2026-08-21：**交接断点事故与一句话修复**——平板电脑轮实测新契约下运行在搜索层"状态汇报"处自动停止：132 次搜索、raw.json 完整落盘后，模型按输出节"文本返回只做状态汇报"的交接措辞结束回合，后处理从未执行（停点离流程终点只差一条脚本命令，用户手动补跑即产出完整 bundle）。复盘：① 两层是同一模型同一上下文的假边界（Skill 工具内联加载），"交接"靠自觉不可靠——旧契约靠"返回数据逼着转录"形成续接压力，新契约数据直接落盘后压力消失；② 修法按用户决策取最小改动：搜索层输出节末新增"收尾"段（写完 raw.json 不要结束回合，立即运行 postprocess 并把 stdout 作为最终汇总展示）+ allowed-tools 加 Bash；编排层 Step 4-6 收缩为指针与无动作说明。skill 完成点移回流程最后一步（与单 skill 时代同理：指令链无中间停靠点）。测试 81 个不变。
- 2026-08-21：**"来源搜索"列锚点归因丢失修复**——平板轮产物实测 36/84 条清单"来源搜索"为空：留痕结构化结果 URL 带 `#数字` 引用锚点，first_query_by_source 用剥锚点后的子串做边界匹配，`#` 属 URL 合法字符集，剥过的 URL 被判定为"更长 URL 的前缀"而拒绝（溯源表不受影响——它走结构化解析先剥锚点再比对，口径差异由此而来）。修法：first_query_by_source 改为与 build_lineage 同路径——结构化结果解析优先（先剥锚点再比对），仅摘要文本提及的 URL 走原文边界匹配兜底（先试未剥原形、再试剥后形态）；与溯源表口径一致（清单"来源搜索"= 溯源.csv 该 URL 第一行查询词）钉为测试不变量。测试 81 → 83 个全过。历史产物（平板轮空值）如实保留不回头改。
- 2026-08-21：**单篇提取放开 + 多语言版本审计（用户决策）**——平板轮 63/132 次搜索零提取复盘发现：搜索层提取时的判定话术"一次性内容不收"让大量相关单篇在提取阶段被丢弃（搜词正确时 10 条结果大半相关，但多为单篇形态——规格页/新闻/问答/博客）。用户定调：站点/子站合并功能先不做，**单篇也提取**（granularity 如实标单篇级，reason 写依据）；同来源有合集入口时仍优先收入口防重复；仍筛三类：无关噪声/通用平台首页/已收录重复。同轮确认**语言版本偏好**（组长建议 #2 落地）：同一内容多语言版本并存时按 中文 > 英文 > 其他 只收一个（提取时语义判断）。按"提示词约束不可强制"的教训配确定性审计：postprocess 新增 count_multilang_groups（同域名+剥语言码路径段后分组计数，只计数不自动合并——子域语言站是不同内容、自动合并边界复杂，先测量后决策），stats 加"多语言版本并存组"列 + stdout 提示，钉进测试。测试 83 → 87 个全过。
- 2026-08-21：**知识清单限制放开 + 降级验证（用户决策：数量为主）**——用户审查发现知识清单三道限制（每叶 2-5 条/总量 ≤50/条目仅体系级）是质量优先时代的产物；当前取向产量优先，且管道已有天然过滤（验证搜索不通过→已尽力→不进 CSV；证据链兜底 URL），清单预筛属多余。落地：① 删条数上限（尽量列全，清单自检保留）；② 体系级与单篇级都可列；③ 验证通过标准按条目粒度对应并支持**降级验证**（方案 B：体系级清单项先找入口、找不到入口时以搜到的体系内单篇验证通过并修正 granularity 为单篇级，reason 注明；连单篇都没有才"已尽力"）——"列什么就收什么"，替代原"入口算过、单篇记顺路"的分账逻辑。预期：搜索量升至 ~170-200 次/运行（约 35-45 分钟），清单验证率回升（降级验证计入通过），单篇级占比上升。脚本零改动，测试 87 个不变。
- 2026-08-21：**stats 列瘦身（用户审计：无消费者的列删除）**——用户逐列审计 stats 表提出"有什么用"：① 单篇级收录——审计问题"例外是否变成常态"已随单篇放开过时，清单"粒度"列可替代 → 删列与 stdout 行（计数逻辑保留在 summary）；② 脚本处理耗时——0.0 秒无信息量（脚本自身耗时 <0.05s），真正的运行成本在搜索阶段 → 删列与 stdout 行；③ 多语言版本并存组——检测有用但无消费者 → stats 列删，stdout 提示保留（检测逻辑与测试保留）。stats 回到 12 列纯迭代对比形态（表头钉进测试）。测试 87 个不变。
- 2026-08-21：**站点级并入合集级（用户决策：两级高度重叠）**——用户指出站点级（全站即领域垂直门户）与合集级（站内领域板块）操作上高度重叠：当前 granularity 仅信息展示、不关注下游采集策略、合并功能暂缓，两级对流程零操作影响且多一个 LLM 判断维度（"这站算不算垂直门户"）。落地：合法值收敛为 合集级/单篇级 两级；存量"站点级"按非法值归一化为合集级（计入缺声明，测试钉住）；技能与文档同步（"体系级"措辞统一为"合集级"）。是否覆盖全站将来可从 URL 形态再判断（根路径即站点级）。测试 88 个全过。
- 2026-08-21：**增量发现角度多样性契约（产量优化第一档）**——算力服务器轮 97 条复盘：搜索层提取 97 条、sources 仅 45（52 条跨搜索重复被弃）、零提取 39/114——根因是"每节点 4 次 + 每维度限 1 词"的固定模板造成角度同质化（全是"XX 列表 + XX 文档"两个模子）。历史对照：双维度是 08-12 自由搜索翻车（9-16 条、规格页漂移）后的防漂移脚手架（决策 5/7），保留；纯自由搜索不可取（已验证的失败模式）。落地"覆盖契约"：每节点 6 次 = 固定 4 次（双维度×中英文）+ 自由 2 次（**仅中文**，从角度池任选本节点未覆盖的 2 个角度各搜一次；用户定调自由角度限中文——补中文生态覆盖短板）；搜索词必须带发现型词汇、禁裸搜产品名/规格页的纪律保留。预期搜索量 ~140-150 次/运行、新源 45 → 60-80 条。脚本零改动，测试 88 个不变。
- 2026-08-21：**stats 瘦身为纯清单统计表（用户两次审计）**——用户逐列审计提出"毫无意义"：运行级属性（领域/时间戳/模型/总候选/去重移除/最终收录）被重复贴到每行节点行上（违反规范化、无信息量，最终收录列每行同一个总数）。定稿形态：stats = 数据源清单的简单统计——[分类节点, 候选数, 体裁分布] 每节点一行 + 末尾"总计"行（候选数=最终收录、体裁分布=全领域合并）；领域/时间戳在文件名、模型在 manifest、过程健康指标（证据校验移除/清单验证）仅 stdout。表头与总计行钉进测试。测试 88 个不变。
- 2026-08-21：**manifest 删除 sha256 校验和（用户审计：防篡改防谁？）**——用户追问"防谁篡改"：单机单人场景无被防对象（LLM 的真实约束是权限白名单；人工清洗清单是正当操作、改完 hash 即过期成噪声；无传输/共享环节；且有心人可连 manifest 一起改——检测的是意外改动而非对抗性篡改，与证据链同一教训）。落地：manifest 条目收敛为 path/purpose/producer（自描述接口保留），sha256_file 函数与 hashlib 依赖删除，条目键集合钉进测试（测试 88 个不变）。
- 2026-08-21：**manifest 整体删除 + 排障材料入 intermediate/（用户定调产出结构）**——用户澄清"删掉吧"指整个 manifest.json（此前只删了 sha256 属理解偏差）；又指出产出目录主次不分：每天看的只有数据源清单与 stats，其余（搜索日志/溯源/输入快照/留痕）都是发现问题才查的排障材料，却与交付物平铺在根目录。落地：① manifest.json 整体删除（自描述接口等下游真正需要时再加回）；② 搜索日志.csv 与 溯源.csv 移入 intermediate/（与输入快照/留痕同处），**根目录只留两个交付物**；③ 根目录文件集合钉进测试（清单+stats 恰好两个）。测试 88 个不变。
- 2026-08-24：**增量发现 6→8 次/节点（专家反馈应对：以加量换广度）**——电源能源硬件轮专家评审两点：① 分类节点偏少（缺测试仪器/仿真工具/电磁材料）；② 题材偏技术文档（缺期刊/专利/硕博论文）。诊断：① 拆解自检维度缺口——阶段 0 完整性自检补抽象维度"配套生态"（与现有"细分技术/应用场景/层级"同风格，不举领域例子，由模型按领域自行实例化）；② 学术类缺席机制——自由角度名额仅 2 个且角度池开放（"包括但不限于"+5 类 17 示例），模型在工业领域从不选学术角度（新闻推荐轮 35/167 学术查询 vs 电源轮 2/129 佐证领域自适应）。用户定调"根本上量太少"：自由名额 2→4（每节点 8 次），**不固定学术角度**——名额耗尽机制提高学术类被选概率，无散文约束；随后将学术类提至角度池首位、配套生态提至自检维度首位（优先级提示，不做硬性约束）。预算 170-200 → 190-220 次。待验证：下次电源轮实测体裁分布与学术查询数。
- 2026-08-24：**运行目录契约重构（--prepare 预留唯一目录）**——契约审查发现 SKILL.md 前后冲突：运行约定声明输出到 `outputs/{领域词}_{时间戳}/`，阶段 5/6 却固定读写 `outputs/raw.json`，未说明目录何时创建；连续/并发运行会互相覆盖同一 raw.json。方案演进：纯文字澄清（不解决并发）→ 暂存文件（引入隐藏空间、失败残留不可见）→ 用户提出"写进当次运行目录"→ 定稿：流程开始先 `--prepare` 预留 `outputs/run_{时间戳}/`（脚本时钟、同秒后缀、每次运行唯一），阶段 5 写 `<运行目录>/raw.json`，收尾脚本从 raw.json 读领域词后把目录整体重命名为 `outputs/{领域词}_{时间戳}/`（命名逻辑不变、中文零进 Bash 命令行）。净收益：一运行一目录从始至终、并发不互相覆盖、失败残留可见（空目录=失败运行）。旧固定路径仍兼容（父目录不匹配 run_ 模式走原逻辑）。已知边界：同机同域并发仍不保证（证据留痕为会话级共享文件，重做 hook/证据层不值得）。TDD：prepare 唯一性/收尾重命名/时间戳复用/后缀冲突/旧路径兼容 5 个新测试先行。
- 2026-08-24：**来源角色维度 + 覆盖评估（专家反馈第二轮治理：同质化与维度缺口）**——用户提出两点：① 覆盖地图驱动——每节点不只问"搜到多少条"，还核对来源维度覆盖，避免"条目多但全来自一类网站"；② 扩充搜索关键词维度——现有组织形式词/体裁词只回答"什么形态"，不回答"谁发布的"，补"来源角色词"（标准组织/监管机构/政府部门/行业协会/厂商/研究机构/大学/评测机构/基金会/公共数据平台）。落地：① 总则关键词维度 2→3；② 来源角色类进阶段 3 自由角度池；③ 阶段 4 按 6 个核心来源维度（官方组织与标准/厂商与产品生态/学术与研究/开源与社区/数据集与公共数据/行业协会与市场信息）核对覆盖，薄弱表加"来源维度单一"行，扩量轮范围加③（空缺维度定向补搜 ≤4 次/轮）。讨论修正：初版触发条件"≥2 维度空缺"会误伤分散健康节点（扩量轮常态化、无效补搜）→ 改为与"体裁单一"同构的定性触发"来源维度单一"（防同质化，精确命中用户目标）；中文/国际生态不单列维度（横切属性，与语言版本机制重叠）；覆盖地图留在运行内上下文，不进 raw.json/stats（stats 保持 3 列纯清单统计）。已知边界：覆盖核对为模型语义判断、脚本无法强制；补搜可能带入边际价值来源（靠 reason 抽检）。预期：搜索量 +20-40 次/运行（至 200-250），集中型节点（UPS/功率半导体等厂商集中型）体裁分布显著均衡。
- 2026-08-24：**验证搜索改组件池自由组合（原固定模板废止）**——用户指出验证搜索是固定套路模板 `{机构/体系名} 官方文档 / 官网`，且与阶段 1"预期体裁仅用于构造验证搜索词"的声明矛盾（字段声明用途、用途没兑现）。方案演进：加体裁槽模板（仍僵硬）→ 自由构造+纪律（缺词汇表托底）→ 用户提出组件池：查询词按来源实际情况自由组合（每词 2-4 个组件）——来源识别（必带其一：原名/英文名/缩写/别名）、入口意图（官方文档/官网/入口/目录/数据库/标准列表/仓库）、辅助（领域词、地域词和语言词），附四例。定稿理由：组件池=带词汇表的自由组合，兼防漂移（当年自由搜索翻车教训）与灵活性；来源识别必带防丢锚点；2-4 组件上限防过度组合压窄查询；扩量轮"官网/官方文档"角度即旧模板，形成"弹性在前、模板殿后"的回退链。钉测试：组件池/来源识别必带/旧模板废止。
- 2026-08-25：**模型自砍搜索次数的治理（第五次"提示词约束不可强制"变体）**——电源能源硬件 8 次/节点版运行日志显示模型在阶段 0/1 犹豫独白："60 条×1 验证 + 12 节点×8 = ~160 次搜索，too huge"，考虑砍清单至 ~50、合并叶子节点。诊断：非契约数字驱动（预算已从 CLAUDE.md 删除），是模型自发的成本本能——它总能自己算总量并把它当预算。实测该轮结局未砍（62 条清单全验、13 节点×8 做满、320 条候选 +50%），但犹豫每轮结局不定。治理（文本层最优）：① 删成本提示词——"列多列杂的代价只是多几次验证搜索"的"代价"一词正是"each item costs searches"的回声，改为"验证不通过自然过滤，无需以搜索成本为由缩减清单"（理由式）；② 阶段 0 加"不以搜索次数或运行时长为由合并节点（搜索成本由设计承担）"。已否决的替代：写"150-250 次属正常量级"——任何数字都成锚，成本敏感模型会朝下界裁（130 事件同款失败模式）。**结构备牌**（未实施）：批量验证搜索——允许同源清单项合并为一条查询验证（60 条 → 20-30 批），从根上消除"条数=次数"的账；代价是混合查询相关性下降、通过标准判定模糊、溯源归因变粗，仅当文本修复后仍砍清单时启用。真实兜底：stdout"清单核对 X/Y"的 Y 与节点数人工可见。钉测试：无"列多列杂的代价"、两处新措辞在。
- 2026-08-25：**历史自锚定治理（deny + 提示词组合）**——运行日志显示模型"发现历史上有过同领域运行（103503），先读它了解格式惯例与历史覆盖情况"，随后"历史运行确认了本领域惯例：13 节点结构"。转录铁证：模型先独立拆出自己的树（含历史树没有的功率半导体节点、自检考虑发电机/变压器/输配电），读到历史后全部作废，最终产物树与历史树逐字相同（13 节点）——P-004 已否决的"相对基准"被模型自发复活（相对基准：以上次运行产物为基线，自我锚定，初始偏差逐轮强化并极化）。方案演进：① 守卫 hook 全天候拦截（dev 分析产物需每次重启会话换配置，工作流成本不可接受）；② 状态感知守卫（运行中 run_* 存在即全会话拦截——用户明确运行窗口内也会用其他会话分析产物，误伤）；③ 会话级守卫（状态文件 + session_id 归属，80 行 + 15 测试——用户判定太复杂）；④ 定稿：**deny Read(outputs/**) + Glob(outputs*) 关掉转录实证的两条通道（Glob 发现 + Read 读内容）**，SKILL.md 阶段 0 加解释式提示词（模型读历史是以为在帮忙，写清自锚定有害即消动机——提示词管动机、deny 管通道，各管一半）。残余通道如实记录：Bash 读（本机 settings.local 便利项）靠动机消除 + 现有"禁止复合命令核对"纪律；绝对路径/刻意混淆可绕（需明知有害还刻意绕道，单人自用可接受）。日常影响评估：用户 IDE 开文件不受权限影响；dev 会话分析产物走 Bash（python -c/ls/grep 在 local 白名单）不受影响；deny 新会话生效。升级路径：会话级守卫 hook 完整方案留档，复发再上。
- 2026-08-25：**note 值"已尽力"改为"未找到官方入口"（术语规范化）**——用户质疑"已尽力"作为数据标注值不合规范：描述努力不描述状态（读者无法得知机构存在与否、入口找到与否），且与配对值"疑似无效机构"（事实状态）语义不对称。定稿：note 值改为事实陈述——"疑似无效机构"（机构不存在证据）/ "未找到官方入口"（机构存在、入口未找到），全部为状态、无努力色彩。纯术语替换：SKILL.md 四处、postprocess docstring 一处、tests 两处、docs/02 五处；docs/03 历史任务记录不动（历史如实保留）。脚本零逻辑改动（note 为模型自由文本、原样透传）。
- 2026-08-25：**领域分析报告（第三交付物）**——用户需求：数据源发现之后要一份"领域分析"——数据源是输入、领域洞察是产出。需求澄清两轮：① 不是统计汇总（stats.csv 已干"数字"的活）；② 不是来源清单解读，是分析领域本身（技术格局/产业生态/趋势观察，类似行业研究报告）。设计：收尾最后一步（跑 postprocess → 展示 stdout → 按模板写报告），无新阶段、无停靠点；模板按 skill-creator progressive disclosure 规范放 references/分析报告模板.md（用户指出初稿模板内联进 SKILL.md 不合规范，改为指针+references）；模型 Read 模板后 Write 到输出目录根（第三交付物）。两条防编造纪律：依据锚定（每段结论标明"从 XX 节点的 XX 来源可见"）、免责声明（模型解读不保证事实准确）。脚本零改动（报告为模型产物，脚本仍只写清单+stats 两个文件，钉测试注释同步口径）。CLAUDE.md 分层索引恢复 references/ 行（目录从预留变真实）。
- 2026-08-25：**阶段 4 终止条件对齐定案机制**——审查发现终止条件"清单全部验证通过"与阶段内定案机制矛盾：定案为"未找到官方入口"的清单项永不"验证通过"，常规运行下该条件永远无法满足。落地：改为"清单项全部了结（验证通过或已定案）"，删除冗余括弧"连续两轮无进展"；扩量轮上限明确为全局 2 轮、优先于薄弱判定（某节点两轮后仍薄弱视为收敛并如实报告，避免无限循环）。钉测试：新表述在、旧括弧不在。单元测试全过。
- 2026-08-25：**"阶段 0 前"改名"初始化"**——预留运行目录步骤旧名"阶段 0 前"易误读为阶段序列的一部分（与阶段 0-6 编号体系冲突）。改法 A（用户选定）：改名"初始化 · 预留运行目录"，流程图同步为"初始化（预留运行目录） → 0 领域拆解 → …"；docs/02 阶段编号同步为合并后的 0-6。钉测试：新标题在、旧名不在。单元测试全过。
- 2026-08-25：**skill 全局文本审查 14 项修复（skill-creator 规范）**——用户要求全局扫描不规范表述与前后矛盾表述，发现 14 项：矛盾 4 处（初始化 Bash 范围、定案时机、剔除语义等）、非正式措辞 6 处（乱搜/掺长尾垃圾/不死循环/纪律保留/不花一次搜索等）、重复规则 3 处、裸禁令 1 处（历史自锚定补理由）。同时阶段 2 重组（查询词构造/按来源语言/每项只搜 1 次分组归位，双语来源可原名+英文名并搜，"只搜 1 次"全文唯一）、分析报告模板正式化（固定模板命令式、去英文全大写 ALWAYS、去与写作纪律重复句）。钉测试：坏词清单断言不在、新措辞在。单元测试全过。
- 2026-08-25：**开头→初始化结构重排（skill-creator 规范）**——用户指出文件头到"初始化"之间内容组织混乱。重排为三段：分工与边界（分工原则/运行期边界）、参数与运行约定（参数表/领域词/输出目录）、通用纪律（搜索词构造/证据链与写入纪律/提取规则/失败路径）；流程图带阶段编号成为全文地图；"总则"改名"通用纪律"（原标注"阶段 2-4 适用"与内容矛盾）；证据链标题注明脚本强制校验（解释为什么硬）；厂商/产品名规则并入词类清单第 4 条（按意图区分，单一归属），阶段 3 改指针，❌/✅ 图标移除。钉测试：新结构标题在、旧标注与图标不在、"裸搜产品名"全文唯一。单元测试全过。
- 2026-08-26：**阶段 0-4 统一模板重构**——用户指出阶段 1/2/3/4 结构不统一、描述不统一、想到什么写什么。落地统一模板：每阶段按加粗分组组织（阶段 0：拆解原则/完整性自检/输出；阶段 1：列什么与列多少/准入判据/清单自检/预期体裁/输出；阶段 2：查询词构造/按来源语言/每项只搜 1 次/通过标准/通过后/未通过；阶段 3：搜索次数/角度池/查询词构造/提取纪律；阶段 4：薄弱判定/未验证项定案/终止条件/扩量轮范围）；角度池六行拆分（学术类/组织形式类/体裁类/数据类/生态类/来源角色类）；标题统一带括号规则、口语措辞清除（"白烧"等）。钉测试：各阶段关键结构断言。单元测试全过。
- 2026-08-26：**source_type 标签治理（源头1：词汇规则）**——用户对比最新两轮运行发现体裁分类一轮很少、一轮很多很杂且同义分裂（学术期刊/学术论文分成两类），历史标签 12-92 类波动。归因：标签自由发挥 + 模型执行方差。治理定两个源头：源头1（本次实施）——标签只能从通用纪律词类词汇（组织形式词/体裁词/来源角色词）中选取；一个词覆盖不了时允许两个词类词汇拼合（如"评测认证"）；不加领域名（"航天行业标准"归"行业标准"）；不另造同义新词（同义分裂让体裁分布无法横向对比）。源头2（脚本侧白名单+规范化）暂缓。钉测试：规则三句在。单元测试全过。
- 2026-08-26：**分析报告命名归脚本（--rename-report）**——用户发现三交付物命名不一致：清单与 stats 带领域时间前缀，分析报告固定叫"分析报告.md"。首版方案让模型按命名规则写文件，用户纠正：确定性命名是脚本的活（分工原则）。落地：模型只 Write 内容到输出目录"分析报告.md"（内容由模型写、文件名不自行命名），收尾最后一步 `--rename-report` 由脚本重命名为 `{目录名}_分析报告.md`。钉测试：--rename-report 在、模板命名纪律在。单元测试全过。
- 2026-08-26：**证据留痕按会话隔离（并行运行互删证据修复）**——用户并行跑两轮发现一轮证据链断裂（"hook 读取不到，另一轮处理后直接删除"）。根因：留痕为共享固定路径 outputs/search_log.jsonl，并行两轮同写一个文件，先收尾的一轮删除共享文件、另一轮证据全失。修法：留痕路径按会话隔离——log_tool.py 与 postprocess 用同一规则从 CLAUDE_CODE_SESSION_ID 派生 `outputs/search_log_{会话}.jsonl`（无该变量回退旧共享路径，兼容手动调用/测试），hook 命令去掉固定路径参数；各会话各写各删，互不销毁。钉测试：路径推导、子进程落盘、hook 命令无固定共享路径。真实并行验证待下次并行运行确认。单元测试全过。
- 2026-08-26：**搜索配额 200→1000/会话**——运行日志报"本次会话 WebSearch 额度用尽（200/200 次）"致扩量轮被砍。核查：非本项目设置，是 Claude Code 内置会话配额（环境变量 CLAUDE_CODE_MAX_WEB_SEARCHES_PER_SESSION，默认 200，版本升级引入——历史会话曾单日 956 次无拦截）。落地：项目级 settings.json env 提至 1000，随 git 分发、所有使用者受益。实证：改后会话 210 次搜索零拦截。钉测试：settings env 断言。单元测试全过。
- 2026-08-26：**节点搜索画像（阶段 0 新增）**——用户手改 skill 新增：每叶子节点补充轻量画像（节点边界：减少跑题与节点间重复；核心搜索词：中英名/缩写/别名；相关来源类型；相关来源角色），供阶段 2/3 构造查询词与阶段 4 覆盖检查取用。边界承诺：不改变 nodes 字段契约、不写入最终清单、禁止为填画像编造机构名称（具体机构仍由清单与搜索发现）。两轮修订定稿：阶段 2 查询词构造可按需加入画像组件（不要求全加）；阶段 3 围绕节点名或画像核心词组合发现型角度词，语言规则明确为固定 4 次中英分别、自由 4 次仅中文（消除与"仅中文"的冲突）；阶段 1 自检指针化（零清单项节点在阶段 3 依靠画像构造查询）；阶段 4 判定表落地画像引用（空缺维度是否适用按画像来源角色判断）；两处自检改指针式复查消除重复罗列。钉桩测试待补。单元测试全过。
- 2026-08-26：**本地旧 hook 残留清理**——用户发现 outputs/ 下两个相同大小的 search_log 文件。核查：settings.local.json 残留并行 bug 排查时代的旧 hook（固定路径版），与项目级新 hook（会话隔离版）并存，每次搜索双写两份；新版收尾只删会话文件，旧共享文件永久残留。落地：删除 settings.local.json 的 hooks 块（项目级配置已覆盖；改动新会话生效，不影响在跑会话），旧残留文件待运行结束后删除。经验：settings.local.json 不入库，git 状态看不到，配置排查必须覆盖本地文件。
- 2026-08-26：**清单生成秩序三项（覆盖标记 / 验证优先级 / 生成顺序）**——用户手改 skill：① 生成顺序——按叶子节点逐项生成，先看该节点搜索画像，再从适用的来源类型和来源角色出发列出权威源，不平铺全领域机构再被动分配；② 覆盖标记——每个来源类型和来源角色标记"已列 / 已检查 / 不适用"（"已检查"=考虑过但暂未找到明确候选，不等同于遗漏），禁为填满维度编造机构，标记不写入契约；③ 验证优先级——候选来源标"核心 / 重要 / 补充"用于阶段 2 验证调度，同样不写入契约。配套：阶段 2"每项只搜 1 次"改为按优先级顺序验证（同优先级按原清单顺序）；阶段 4 判定表"空缺维度明显不适用时不补"的例外改以覆盖标记"不适用"为准（判断依据从画像收敛到标记一处）。两轮修订定稿：覆盖标记补"不写入契约"边界声明（与验证优先级对称）；清单自检改指针式引用消除三态重复。预期效果（推演）：清单更均衡、零清单项节点减少；验证段变长、扩量段变短（"不适用"维度不再补搜）；优先级对完整运行结果几乎无影响，收益在中断/自砍场景（核心先验、底线先钉，D6 同逻辑）。边界：三项均为流程内部软约束（不落盘、脚本不可见、产物无痕迹），执行打折只损失收益、不产生坏输出；钉桩测试待补。单元测试全过。**（2026-08-27 已整体回退，见下条）**
- 2026-08-27：**清单生成秩序三项回退（revert 26ca1a1）**——用户定调回到交换机 229 条版本（画像版）。证据回顾：① 验证优先级两轮实证未执行（验证段严格按清单原序，交换机/电源各一轮）；② 覆盖标记效果证据矛盾（交换机扩量 22→12，电源 30 为规则后最高）；③ 生成顺序有正向证据（两轮零清单节点为 0、每节点 3-7 条）但随提交整体回退——简单优先，需要时可单独恢复；④ 产量下降（229→126→169）主因是节点/清单执行方差（历史 7-18 节点、26-87 清单波动），非该提交直接因果。教训：新规则上线后至少跑两轮再评估；产量类指标先排除拆解/清单方差再归因。
- 2026-08-27：**增产改造与四洞修复**——① 每节点 8→12 次（自由 4→8 仅中文，优先覆盖不同来源类型/来源角色、避免同类角度重复换词）；新增"易失效角度"规则（论文/专利/标准裸搜命中通用平台首页被拒——08-27 上午交换机运行查询词实证）。② 扩量轮从全局 2 轮改为按节点独立判断（连续一轮无新增即收敛；总条件改"非薄弱或已收敛"——修复体裁单一节点使"所有节点非薄弱"字面条件永不满足的矛盾，与 08-25"全部验证通过"同类病）；删除"总搜索次数 1000 全局停止"句（用户定调不设配额上限，系统默认值即上限）。首轮实证（交换机 08-27 下午）：230 条与历史最高持平（11 节点）、每搜均 1.19 创新高（增量阶段 1.46、中文每搜 1.50）、零提取 26%、标签 17 类长尾仅 1、证据缺失 0；节点内位置拆解显示自由中文角度（5-12 位，每搜 1.66/1.57）显著优于固定 4 次（1.16），12 次无饱和。深挖发现四洞并修复：③ 提取端整行拒收——Nokia 7250 查询返回 10 条官方文档页提取 0，52 个零提取行合计 451 条未收录结果（真货估 40-70 条）→ 新增"逐条判断（禁止整行拒收）"规则并澄清"优先收合集入口"歧义（只在合集与单篇同时出现时生效）；④ 英文裸后缀全废——22 次英文增量 45% 零提取（`X list` 模式返回书店页/股票新闻等垃圾页而非平台首页）→ 易失效角度规则扩展英文；⑤ 验证首轮 42% 落空（官网式重试可救但不保证）→ 首轮优先"来源识别 + 官网/官方文档"；⑥ 薄弱节点维度窄（运营商级 10 条 vs 学术文献 49 条，零提取查询返回结果里确有合格源未收）→ 画像核心搜索词补"行业术语与细分场景词"。钉桩：新增 2 个测试 + 画像基础钉桩补齐，单元测试全过。**结构后备入待办账本：盘点轮**——散文"逐条判断"若下轮仍现整行拒收，实施脚本盘点（列未收候选、模型逐条补判定）；另：230 为交换机域当前通道的质量天花板（搜索返回池 814 域名/1709 URL，合格成体系源约 230-300），突破需实体深挖或跨域。


